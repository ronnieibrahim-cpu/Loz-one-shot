# Audited overworld rooms

One line per overworld room a session has actually looked at for region-art
consistency (STATE.md rotation objective #6). `Region` is free text — no
canonical region partition exists elsewhere in the data (see
`tools/check-drift.mjs`'s own comment on this), so use whatever name the
prose in `docs/ART-BACKLOG.md` already uses for that stretch of coast if one
exists, and be consistent with earlier rows before inventing a new one.
`Verdict` is one sentence: what you checked and what you found, not a
restatement of the room's contents.

`tools/check-drift.mjs` reads this file; do not hand-edit its table
structure without checking the column order still matches what it parses
(key, region, name, date, verdict).

| Key | Region | Name | Date | Verdict |
|---|---|---|---|---|
| 0,4,7 | Tidewatch Village | Tidewatch Village | 2026-09-14 | Checked against ART-DIRECTION.md at all 3 tides (identical — screen sits above the tideline); house, tree crowns, bushes, stump, chests, rocks and sign all read as clean extractions with solid outlines and no mixed registers; no defect found. |
| 0,5,7 | Tidewatch Village | Village East | 2026-09-14 | Checked at all 3 tides — the sand/water tide patch on the east side composites a proper jagged edge into the grass at every level (no hard pixel cut), and Mirren's newly-assigned npc_fisher2 sprite reads as a distinct person beside the shop and tree crowns; no defect found. |
| 0,4,8 | Tidewatch Village | Village Shore | 2026-09-14 | Checked at all 3 tides — the dry/wet pool edge composites cleanly (zoomed crop), crab and shoreSalter (FOLK.salter) extractions both clean; no defect found. |
| 0,5,8 | Tidewatch Village | Driftwood Strand | 2026-09-14 | Checked at all 3 tides — sand/water channel edges composited correctly; a smaller dark-teal bush beside a tree crown is a genuinely different decoration type, not a palette mismatch (zoomed and compared); octorok, gem pickup and the wooden ledge structure all read clean; no defect found. |
| 0,3,7 | Tidewatch Village | West Bluff | 2026-09-14 | Checked at all 3 tides — cave entrance, ledge and sign all read clean; the tide pool's sand-facing edges are a hard cut with no rim, but this matches the already-documented "narrow channel" limitation in ART-BACKLOG.md's shore-rim entry (water one tile wide with land on both sides), not a new defect. |
| 0,6,7 | Tidewatch Village | Sunken Reef | 2026-09-14 | Checked at all 3 tides — large HIGH-tide lake shows a proper composited shallow-water rim against sand; the hard-edged inner corner is deep water (waterD), which the shore-rim fix deliberately excludes per ART-BACKLOG.md; no new defect found. |
| 0,7,7 | Tidewatch Village | Shallows Gate | 2026-09-14 | Checked at all 3 tides — palm trees (zoomed) have a clean outline and flat 3-4 colour shading matching the register; sand-ripple dithering and the composited shallow-water rim both read correctly; no defect found. |
| 0,3,8 | Tidewatch Village | Shell Beach | 2026-09-14 | Checked at all 3 tides — grass/sand boundary composites correctly (zoomed); the cross-shaped HIGH-tide water is deep (hard edge against both sand and grass, matching the documented waterD exclusion); no new defect found. |
| 0,6,8 | Tidewatch Village | East Strand | 2026-09-14 | Checked at all 3 tides — sand/grass boundary and tide-pool growth both read clean, crab and tree/bush decorations consistent with the established register; no defect found. |
| 0,7,8 | Tidewatch Village | Dune Crossing | 2026-09-14 | Checked at all 3 tides — palm corners and sand-ripple dithering match Shallows Gate's; gel enemy and flower decorations read clean; deep-water hard edge again matches the documented waterD exclusion; no defect found. |
| 0,2,7 | Marsh | Bog Causeway | 2026-09-14 | Checked at all 3 tides — the marsh ground (grassBog) is a genuinely distinct dark-olive reed texture, not grass recoloured (zoomed and compared), and its shallow-water edge correctly shows the composited rim (zoomed, confirmed on both the horizontal and vertical edges); no defect found. |
| 0,2,8 | Marsh | Sunken Reeds | 2026-09-14 | Checked at all 3 tides — grassBog/grass/sand three-way boundary composites cleanly (zoomed); shallow water rims correctly at LOW, water goes deep (hard edge, matches documented waterD exclusion) by HIGH; keese and the gem pickup read clean; no defect found. |
| 0,8,7 | Dunes | Grotto Approach | 2026-09-14 | Checked at all 3 tides — same sand-ripple/palm-corner look as Shallows Gate/Dune Crossing (region-art's prior "Tidewatch Village" batch); the `dunes` legend sharing `coast`'s visual palette is a deliberate shared desert aesthetic, not a copy-paste mismatch (both read as one consistent sandy register). Ledge/dock tile at the south edge reads clean; no defect found. |
| 0,8,8 | Dunes | Grotto Mouth | 2026-09-14 | Checked at all 3 tides — the stone grotto entrance (arched doorway, brick texture) is a clean, well-outlined structure distinct from a plain cave mouth; shallow-water rim at LOW correctly degrades to a hard edge once deep by HIGH; no defect found. |
| 0,4,9 | Tidewatch Village | Fishing Stones | 2026-09-14 | Control check, same `coast` legend as the first 10 rows, this far from the village centre — sand-ripple dithering, grass/sand boundary and deep-water edge all consistent with the earlier batch; wandering villager sprite reads clean; no defect found. |
| 0,5,9 | Tidewatch Village | South Sands | 2026-09-14 | Checked at all 3 tides — same `coast` palette holds up; a leever-shaped enemy correctly straddles the shallow/deep water line as the tide rises; no defect found. |
| 0,1,7 | Marsh | Sanctum Path | 2026-09-14 | Checked at all 3 tides — the mudflat furrow texture reads as a genuinely different tilled-earth pattern from the marsh's own grassDark, and the sandbar tide tile correctly goes dry sand at LOW, wadeable at MID, deep at HIGH; the vertical ledge run in the mudflat composites as a continuous stone-faced drop with no seams; zol enemy and gem pickup both clean; no defect found. |
| 0,1,8 | Marsh | Sanctum Mouth | 2026-09-14 | Structure check (Bogwater Sanctum's portal, `block:portalD3`) — the moss-capped stone archway is a clean, well-outlined extraction distinct in register from a plain cave mouth, and its moat rises through all 3 tides in step with the sandbar/mudflat pattern established elsewhere; sign, tree, rock and the zol enemy all read clean; no defect found. |
| 0,9,7 | Dunes | Dune Bowl | 2026-09-16 | **Revised.** S64's verdict cleared the vertical `ledgeSandW` run as legitimate art (the rotated-`ledgeS` sprite, not a broken asset) but never asked whether a ledge belonged there at all — it sat in the middle of a quicksand patch with plain sand on every side, no cliff anywhere in the room, which the source games never do (new CLAUDE.md rule). Removed the 4-cell run, closing the quicksand patch into one continuous shape; `check-strands.mjs`/`check-overworld.mjs`/`check-placement.mjs`/`check-ground.mjs` all still clean, `npm run build` rebuilt. Re-screenshotted at all 3 tides — both leever enemies and the tide-flooding corner still read clean. |
| 0,9,8 | Dunes | Sandpiper Row | 2026-09-14 | Checked (no tide variation — screen sits above the tideline) — the red/shuttered-green house blocks, sign, crab, wandering fisher1 (`npc_fisher`) and Sennit the trader (`npc_hood_blue`) all read clean at 4x zoom, hood-shaped trader silhouette included. Correction to NEXT-PROMPT.md's premise: the `npc_elder`-sprited `sandpiper` NPC (S61 reassignment, docs/NPCS.md) lives inside the `houseSandpiper` interior, not on this overworld screen — there is no elder here to check; not a defect, just a wrong room reference. |
| 0,3,9 | Tidewatch Village | South Bluff | 2026-09-14 | Checked at all 3 tides — the buried-rupee digSpot mound (fixed terrain, not a tide tile) stays dry as the surrounding sandbar floods, ending as a one-tile sand peninsula with a hard edge on 3 sides at HIGH; matches the already-documented narrow-land-in-water hard-edge exception (S65's West Bluff), not a new defect. Octorok, rocks and tree crowns all clean. |
| 0,6,9 | Tidewatch Village | Reef Pocket | 2026-09-14 | Checked at all 3 tides — the seafloor tile (`4`, walkable only at LOW) correctly shows as a distinct mottled dry patch at LOW and submerges under wadeable/deep water at MID/HIGH; the rupee pickup floats visibly over water at every tide; sign, bushes and rocks clean; no defect found. |
| 0,0,7 | Marsh | Mire | 2026-09-14 | Checked at all 3 tides — mudflat/sandbar edges match the established marsh pattern; a leever spawning next to Link's default screenshot position produced a stray horizontal cyan bar under him at HIGH tide, confirmed by re-shooting from a different `--px/--py` that it is a transient contact/attack effect tied to the coincidence of that spawn point, not a terrain or sprite defect; tree crowns and rock clean. |
| 0,0,8 | Marsh | Deep Mire | 2026-09-14 | Checked at all 3 tides — sand/water edge on the west side composites correctly at every level; two leever enemies and the dark bush cluster (top right) read clean and distinct from plain tree crowns; no defect found. |
| 0,1,6 | Marsh | Bog Stair | 2026-09-14 | Checked at all 3 tides — the cracked cliff wall at the north edge (`cliffCrackedDk`) reads as a distinct crack pattern from the plain `cliffDk` either side of it, consistent with the sign's "boulders, and the cliffs beyond" hint; mudflat furrows, sandbar tide edges and the south ledge (same plank-wall asset as Sanctum Path) all clean; leever and sign clean; no defect found. |
| 0,10,7 | Dunes | Tidepools | 2026-09-14 | Checked at all 3 tides — sandbar/tidePool patches lighten and darken correctly with the tide, including the single `channel` tile at the west edge; crab and urchin sprites both read clean and distinct from each other despite the similar pink/purple spiky silhouette; no defect found. |
| 0,11,7 | Dunes | Far Dunes | 2026-09-14 | Checked at all 3 tides, specifically for the task's own concern (the `***` open-sea cells at the map edge) — open sea renders as a hard-edged, permanently deep blue with no rim, which is correct: it is the world's edge, not a shore, and every other tide tile in the room floods into it progressively and correctly. RockSand boulders and the leever clean; no defect found. |
| 0,3,6 | Tidewatch Village | Bluff Hollow | 2026-09-14 | Checked (no tide variation — screen sits above the tideline) — a fully enclosed tree-crown hollow with the same digSpot mound art confirmed earlier this session (South Bluff), 3 bush/flower decorations, sign and rock all clean; no defect found. Last plain-`coast`-legend room near the village. |
| 0,0,6 | Marsh | Bog Head | 2026-09-14 | Checked at all 3 tides — mudflat furrows, sand/water edge and zol enemy all match the established marsh pattern; no defect found. |
| 0,2,6 | Marsh | Reedbank | 2026-09-14 | Checked at all 3 tides — the plus/cross-shaped tide patch is 3 separate tide tiles (a 4-wide `channel` plus two lone `sandbar` cells above/below), not one shape, and each floods on its own correct schedule (only the channel bar is wet at LOW, the arms fill in at MID/HIGH) — worked out from the room's own grid rather than assumed from the shape. Keese and zol both read clean; no defect found. |
| 0,0,9 | Marsh | Bog Foot | 2026-09-14 | Checked at all 3 tides — sandbar/open-sea edge at the world's south rim is a correct hard cut, matching the already-documented open-sea exception; leever and rocks clean; no defect found. |
| 0,1,9 | Marsh | Witchs Hollow | 2026-09-14 | Checked at all 3 tides — Yarrow the bog witch (`FOLK.kelper`, a round green goggle-eyed creature) reads as a deliberately strange, thematically-fitting design for a "witch" in a hollow, not a mismatched or broken sprite; the two digSpot mounds over the buried heart piece match the mound art confirmed twice already this rotation; flowers and bush clean; no defect found. |
| 0,2,9 | Marsh | Marsh Corner | 2026-09-14 | Checked at all 3 tides — a small dark mark under Link at MID tide is his ordinary swim ripple/shadow, not a terrain artifact (confirmed absent at LOW where he's on dry sand and present at both MID and HIGH where he's in water); mudflat, sandbar edges and zol all clean; no defect found. **Marsh region is now fully audited: 12/12 rooms in docs/AUDITED-ROOMS.md, zero real defects found across the whole region.** |
| 0,7,6 | Dunes | Dune Head | 2026-09-16 | **Revised.** S70's verdict called the horizontal ledge run "clean" but only checked the art itself, not whether it belonged there — it sat in open sand with no cliff anywhere in the room, matching the new "ledges only at a real elevation change" rule in CLAUDE.md, not something the source games do. Removed the 4-cell run and levelled it to plain sand; the dense L-shaped palm treeline, sandbar tide patch, crab and rockSand boulders are otherwise unchanged and still clean. `check-strands.mjs`/`check-overworld.mjs`/`check-placement.mjs`/`check-ground.mjs` all still clean, `npm run build` rebuilt. |
| 0,8,6 | Dunes | North Dunes | 2026-09-14 | Checked at all 3 tides — the flat solid-black chasm (`J`, Roc's Feather gate) is confirmed-deliberate art per tiles-core.js's own comment (a uniform body avoids a false "4 separate holes" illusion a shaded one produced); sandDeep quicksand patch, leever and crab clean; no defect found. |
| 0,9,6 | Dunes | Sandbar Run | 2026-09-14 | Checked at all 3 tides — the asymmetric flooded-puddle shape (wider at the top row than the middle rows) matches the room's own grid exactly; octorokSea (aquatic-only) is absent at LOW and present at MID/HIGH, which is enemy spawn logic, not an art or rendering defect; crab clean; no defect found. |
| 0,10,6 | Dunes | Feather Gap | 2026-09-14 | Checked at all 3 tides against the room's own sign ("the gaps are a single stride wide") — 4 separate fixed-deep-water blocks sit with single-tile dry gaps between them at LOW, and read as one connected body once the sandbar tiles between them flood at MID/HIGH; sign and rocks clean; no defect found. |
| 0,11,6 | Dunes | East Dunes | 2026-09-14 | Checked at all 3 tides — rockSand boulders, the open-sea hard edge at the world's east rim, leever and the rupee pickup all clean; no defect found. **Dunes' whole north row (7,6-11,6) is now audited, all 5 rooms clean.** |
| 0,7,9 | Dunes | South Shallows | 2026-09-14 | A first visual read suggested deep water starting right under Link at LOW tide, which would contradict "sandbar is dry at LOW" — checked by querying `room.tile(tx,ty,tide)` directly in-engine instead of trusting the screenshot crop, and the room genuinely has 2 full dry sand rows before its fixed-deep rows begin; the visual impression was wrong, not the tide system. Crab and palm trees clean; no defect found. |
| 0,8,9 | Dunes | Wrecked Hull | 2026-09-14 | Checked at all 3 tides — the 3 `post` (`F.SNAG`) decorations read as stubby wooden mooring posts, not fence stakes, which fits "Wrecked Hull" thematically and is a legitimate reuse of the dungeon `dPost` asset outdoors; the wreckSurvivor trader (`npc_villager`) and crab both clean; no defect found. |
| 0,10,8 | Dunes | Shell Flats | 2026-09-14 | Checked at all 3 tides — the seafloor patch (walkable only at LOW) submerges correctly at MID/HIGH; urchin and the heart-piece pickup both clean; no defect found. |
| 0,11,8 | Dunes | Dune Corner | 2026-09-14 | Checked at all 3 tides — the leever sitting on Link's default screenshot position at HIGH tide is the same spawn-point coincidence already seen and cleared this rotation (S70's Mire), not a new artifact; digSpot mound, rockSand boulder and open-sea edge all clean; no defect found. |
| 0,9,9 | Dunes | Deep Bar | 2026-09-14 | Checked at all 3 tides — seafloor submerges correctly; urchin and octorokSea (aquatic-only, absent at LOW/present at MID-HIGH, per its own spawn logic) both clean; no defect found. |
| 0,10,9 | Dunes | Sunken Cove | 2026-09-14 | Checked at all 3 tides — first appearance in this rotation of the siren enemy (a blue humanoid with a white bubble/splash particle trail); reads as a clean, distinct silhouette in register with the rest of the cast, not a placeholder or broken effect. Channel tide tile and octorokSea both clean; no defect found. |
| 0,11,9 | Dunes | Worlds Edge | 2026-09-14 | Checked at all 3 tides — sign ("Nothing past here but open sea"), rockSand boulders and the open-sea hard edge on 3 sides all read as a deliberate "end of the map" screen; no defect found. **Every dunes-legend room labelled "Dunes" is now audited: 18/18 in that grouping (2 more dunes-legend rooms, Shallows Gate and Dune Crossing, were already done under the "Tidewatch Village" label back in S65), zero real defects across the whole legend.** |
| 0,4,6 | Wood | South Wood | 2026-09-14 | First room checked in the Drowned Wood, flagged by NEXT-PROMPT.md for the historical tree-crown-severing risk (CLAUDE.md's hard-won-lessons list) — ran `node tools/check-strands.mjs` in addition to the usual visual pass and it stayed at the established baseline (15 regions/24 cells, no new multi-cell region), so this room introduces no connectivity defect. Visually: `treeDark`'s darker oak palette (`treeoakdk` vs `treeoak`, confirmed in src/gfx/palettes.js) reads as a genuinely muted variant of the same tree, not a mismatch; `tideGrass` (the `0` digit, first seen this rotation) correctly stays dry grass through MID and only floods at HIGH, distinct from the sandbar tiles flanking it which flood a tide level earlier; mud patch and zol clean. No defect found. |
| 0,5,6 | Wood | The Wading | 2026-09-14 | Checked at all 3 tides — a fixed deep-water core (`=`) ringed by sandbar that wades in at MID and drowns at HIGH, matching the room's own sign ("Only a swimmer goes north from here"); sign and rock pair clean; no defect found. |
| 0,6,6 | Wood | Wood Foot | 2026-09-14 | Checked at all 3 tides — a keese (flying) casts a grey ground shadow directly beneath it, confirmed in `src/game/enemy.js` (`this.shadow = this.flying`) rather than mistaken for a stray prop; the rupee20 pickup reads as a legitimate gem-shaped treasure, not an unauthored icon; mud patch and sandbar-into-channel flood correctly across tides; no defect found. |
| 0,4,5 | Wood | Bog Trees | 2026-09-14 | Checked at all 3 tides — zol's upright teal-capsule sprite and keese both read clean and distinct; the small grey blob near the keese is confirmed (same shadow mechanism as Wood Foot) to be the keese's own flying shadow, not a terrain defect; sandbar patches flood correctly; no defect found. |
| 0,5,5 | Wood | Wood Heart | 2026-09-16 | **Revised.** S72's verdict read the `ledgeDk` run as a "dark plank walkway" and moved on without asking why a one-way drop sat in the middle of open grass with no cliff anywhere in the room — the source games never place one there (new CLAUDE.md rule). Removed the 4-cell run and levelled it to plain grassDark. The trader's `npc_hood_blue` sprite, wisp's ghost sprite and shadow, and the tideGrass/sandbar flood order are otherwise unchanged and still clean. `check-strands.mjs`/`check-overworld.mjs`/`check-placement.mjs`/`check-ground.mjs` all still clean, `npm run build` rebuilt. |
| 0,6,5 | Wood | Sunken Glade | 2026-09-14 | Checked at all 3 tides — zol and moblin both read clean and distinct. A green sprout briefly visible above the top tree row at MID/HIGH but not LOW was checked in-engine rather than eyeballed: `room.tile()` shows the bordering `grassDark` cell carries a randomised `grassTuft` variant (`variants`/`family:'shore'` on the tiledef) that the shore-adjacent rendering picks when its neighbour is actually wet — a reed-at-the-waterline detail, not a rendering glitch. Channel tile floods on the correct low-tide-only schedule; no defect found. |
| 0,7,5 | Wood | Wood Verge | 2026-09-14 | Checked at all 3 tides — anglerfry sits in a `waterD` cell directly above a treeline, so its lower half visually reads as sitting on the tree canopy; traced this to `Room.quadCanopySolid` (src/world/room.js), whose own comment describes exactly this case ("a canopy over a stream is a branch reaching over the water, and swimming under it is what the source draws") and which explicitly keeps a WET cell's overhang passable at every tide — confirmed this is the deliberate source-accurate rendering, not an unreachable or overhung enemy. Two `waterD` pools separated by a walkable mud/sandbar causeway, matching the "Verge" name; no defect found. |
| 0,4,3 | Wood | Wood Edge | 2026-09-16 | **Revised.** S75's verdict confirmed the ledge run was a legal, safe placement (per `find-ledges.mjs`) but not whether it matched the source's own placement grammar — it sat in the middle of open mud with no cliff anywhere in the room (new CLAUDE.md rule). Removed the 4-cell run and levelled it to plain mud. tideGrass/sandbar flood order and zol are otherwise unchanged and still clean. `check-strands.mjs`/`check-overworld.mjs`/`check-placement.mjs`/`check-ground.mjs` all still clean, `npm run build` rebuilt. |
| 0,5,3 | Wood | Rotting Grove | 2026-09-14 | Checked at all 3 tides — wisp's smiling-ghost sprite and keese (with its own flying shadow, confirmed mechanism from S72) both read clean; the cliff/plank structure at the south edge is `cliffDk`, matching the room's `##` legend chars; tideGrass and sandbar flood on their usual separate schedules; no defect found. |
| 0,6,3 | Wood | Wood Gate | 2026-09-14 | Checked at all 3 tides — the 3-rock cluster in the middle has a sandbar tide tile between two of the rocks, not 3 decorative rocks in an unbroken line, so it doesn't trip the "no 3 decorative props in a row" rule; moblin and sign both clean; no defect found. |
| 0,4,4 | Wood | Shrine Path | 2026-09-14 | Checked at all 3 tides — the default screenshot spot puts Link almost on top of zol's own spawn tile, and zol's tall sprite pokes up over Link's head, briefly reading as odd-coloured hair; re-shot with Link a few pixels south (the same "spawn-point coincidence" already cleared for Dune Corner, S70) and zol reads as its ordinary clean teal capsule on its own. Keese (with shadow) and the tree/sand layout otherwise clean; no defect found. |
| 0,5,4 | Wood | Shrine Mouth | 2026-09-14 | Checked at all 3 tides — the D5 dungeon gate (stone archway, brown door, carved emblem) reads as a proper, distinct dungeon entrance, not a placeholder; the topiary-like bush beside it, sign and wisp all clean; tideGrass/sandbar in front of the gate flood on schedule; no defect found. |
| 0,6,4 | Wood | Log Drift | 2026-09-14 | Checked at all 3 tides — anglerfry sits inside the channel tile itself this time (no canopy overhang nearby), correctly wadeable at LOW and deep from MID; the heart-piece pickup sits on the dry mud causeway, reachable on foot; no defect found. |
| 0,7,3 | Wood | The Gyre | 2026-09-14 | Given its own careful pass per CLAUDE.md's hard-won-lessons list (a tree-crown fix once silently severed this exact room). Checked at all 3 tides — the closed riptide ring (N/E/S/W, permanently deep at every tide, matching "riptide" being letters rather than tide digits) and its mud/ledge centre read exactly as the room's own comment describes: a swimmer is carried around the ring, a sinker (Cleats) walks straight through the middle. `check-strands.mjs`'s baseline 10-cell region for this room's centre is that same centre, unreached by the checker's ON-FOOT-ONLY flood — not a severed corridor, the room's whole point. Sign, cliffs and anglerfry all clean; ran `check-strands.mjs` again after looking and it's still exactly the established baseline; no defect found. |
| 0,7,4 | Wood | Drowned Hollow | 2026-09-14 | Given its own careful pass per CLAUDE.md's hard-won-lessons list (a tree-crown fix once silently severed this exact room's north doorway). Checked at all 3 tides — three fixed `waterD` bands separated by dry mud causeways, identical at every tide (correct: `waterD` isn't a tide digit); anglerfry and jellyfish both read as clean, distinct silhouettes. `check-strands.mjs`'s baseline entries for this room are single border cells only, matching the already-documented "root pocket inside a treeline" pattern, not a reopened severed doorway; re-ran the checker after looking and it's unchanged from baseline. No defect found. **The Drowned Wood is now fully audited: 15/15 wood-legend rooms.** |
| 0,8,0 | Reef | Coral Gate | 2026-09-14 | First reef-legend rooms checked this rotation. Checked at all 3 tides — `g` is `rockFloor` here, not grass, and reads as a genuinely distinct stony ruin floor next to the wood/dunes grass palettes; the 4 crate-shaped `post` decorations sit 2x2, never 3 in a straight line; `reefFlat` (the sand-coloured `6` patch) stays dry-looking at LOW and turns a shallow teal at MID/HIGH, matching "exposed at low"; jellyfish clean; open sea at the north edge is the correct hard, permanent edge. No defect found. |
| 0,9,0 | Reef | Palace Wall | 2026-09-14 | Checked at all 3 tides — `reefDeep` (`7777`) is wadeable-looking teal at LOW and a deeper blue at MID/HIGH, matching "wadeable at low, deep above"; octorokSea is absent at LOW and present at MID/HIGH (aquatic-only spawn logic, same pattern as dunes' `Sandbar Run`), briefly caught mid-attack with its own launch spark at MID — a transient effect, not a static defect; heart-piece pickup and rock-floor border clean. No defect found. |
| 0,10,0 | Reef | Tide Steps | 2026-09-14 | Checked at all 3 tides — `tideRock` (`8888`) stays dry through LOW and MID and only floods at HIGH, one tide level later than the neighbouring `reefFlat` (`6666`), matching each tile's own written behaviour exactly; siren's bubble/splash trail (already confirmed at Sunken Cove) reads clean here too. No defect found. |
| 0,11,0 | Reef | East Spire | 2026-09-14 | Checked at all 3 tides — jellyfish's spawn tile sits one half-tile from Link's default screenshot spot, so it briefly rendered on top of his head at LOW; by MID it had wandered off and reads as its ordinary clean sprite (the same spawn-position coincidence as S70's Dune Corner and S73's Shrine Path). 3 crates split 2-and-1 with a gap between, not 3 in a row; `reefDeep` and the east-edge open sea both clean. No defect found. |
| 0,8,1 | Reef | Reefway | 2026-09-14 | Checked at all 3 tides — the combined sandbar/`reefFlat` cross floods in 3 visible stages (dry sand at LOW, shallow teal at MID, deeper blue at HIGH), consistent with every sandbar room audited so far; the two crabs read clean and distinct. No defect found. |
| 0,9,1 | Reef | Hooked Channel | 2026-09-14 | Checked at all 3 tides — `reefDeep` and `channel` both flood on their own correct, different schedules (channel wadeable only at LOW, reefDeep wadeable at LOW and deep above); octorokSea (aquatic-only) and the 4 crates (no 3 in a row) both clean. No defect found. |
| 0,10,1 | Reef | Palace Mouth | 2026-09-16 | Checked at all 3 tides — the cave-mouth interior drips a white splash particle onto the pool below it, a transient effect rather than a defect; siren spawns and reads clean against the sand/`reefFlat`/channel flood; sign clean. No defect found. |
| 0,11,1 | Reef | Spire Shallows | 2026-09-16 | **Fixed.** A 4-cell `ledgeRock` run sat in the exact middle of an open sand pocket, dry sand on both sides at every tide, no cliff or raised shelf anywhere in the room (new CLAUDE.md rule) — not load-bearing, the sand columns either side of the run already went around it. Levelled to plain sand. Pot and jellyfish otherwise clean. `check-strands.mjs`/`check-overworld.mjs`/`check-placement.mjs`/`check-ground.mjs` all still clean, `npm run build` rebuilt. |
| 0,8,2 | Reef | Sunken Colonnade | 2026-09-16 | Checked at all 3 tides — 4 `post` pillars stand in a 2x2 diamond around a `reefDeep` pool (never 3 in a row), matching the room's own "colonnade" name; octorokSea (aquatic-only) reads clean. No defect found. |
| 0,9,2 | Reef | Reef Market | 2026-09-16 | Checked at all 3 tides — the reefkin trader NPC stands among 4 `bushSand` clumps (2x2, never 3 in a row); `channel` floods on its own low-tide-only schedule in the room's NE corner. No defect found. |
| 0,10,2 | Reef | Drowned Steps | 2026-09-16 | Checked at all 3 tides — the `ledgeRock` run here sits directly below the `tideRock` (`8888`) shelf, a real one-tide-level elevation change into the plain floor south of it, matching the room's own name; this is the pattern the new CLAUDE.md ledge rule is meant to keep, not remove. Siren and barnacle both clean. No defect found. |
| 0,11,2 | Reef | Outer Reef | 2026-09-16 | Checked at all 3 tides — 4 `post` pillars in the same 2x2-diamond motif as Sunken Colonnade (never 3 in a row); jellyfish clean. No defect found. |
| 0,8,3 | Reef | Reef Foot | 2026-09-16 | Checked at all 3 tides — a straight 6-tile run of `palm` (trees are exempt from the "no 3 decorative props in a row" rule per `check-ground.mjs`); the crab visually overlaps the tree canopy directly above it, the same 32x32-tree-overhang rendering already confirmed harmless elsewhere in the ledger. No defect found. |
| 0,9,3 | Reef | Barnacle Bank | 2026-09-16 | The screenshot alone reads as if `reefDeep` never changes shade across tides — queried `room.tile()` and confirmed it does switch `waterSReef` (LOW) -> `waterDReef` (MID/HIGH) as designed, then confirmed with a raw pixel sample (168,232,208 at LOW vs 72,176,152 at MID/HIGH): a real but subtle colour shift, not a static bug. Two barnacles read clean. No defect found. |
| 0,10,3 | Reef | Palace Causeway | 2026-09-16 | **Found a systemic pattern worth naming, not fixing here (no detour token to spend on it).** Queried `room.tile()` at HIGH tide: the room's `sandbar`/`tideRock` cells resolve to plain `waterS`/`waterD` (pal `water`/`deep`), never the reef-teal `waterSReef`/`waterDReef` — because those two tide tiles are shared globals (`src/data/tiles-core.js`) that hardcode the plain palette for every region, not just this one. In this room the plain-blue cells and the teal `reefFlat` cells are kept apart by a dry sand band, so there's no actual seam to see, and this exact mix (plain tide digits beside reef-specific `6`/`7`) is already present, unflagged, in every reef room audited so far (Coral Gate onward) — a pre-existing, pervasive characteristic of the whole region, not something new to this room. Left alone; noted in `docs/NEXT-SESSION.md` for whoever next has a detour token free. Rocks (2, not 3) clean. |
| 0,11,3 | Reef | Reef Edge | 2026-09-16 | Checked at all 3 tides — the visibly growing blue area toward the east edge is the `sandbar` column (dry->wadeable->deep) joining the permanent `openSea` column beside it, not a static-terrain bug; 2 rocks (not 3), fairy pickup and 3 palms all clean. **The reef region is now fully audited: 16/16 reef-legend rooms.** |
| 0,4,0 | Salt | North Pan | 2026-09-16 | First salt-legend rooms checked this rotation. Checked at all 3 tides — `saltFlat`/`saltCrust` (a diagonally-dithered crystalline texture) reads as a genuinely distinct third ground palette next to grass/rock-floor/sand; the `_` ledge run sits directly below a `shoal` (`3333`) patch that reads as a visually distinct raised, undithered terrace at LOW — the same real-elevation pattern as Drowned Steps, not a violation. Two leevers clean. No defect found. |
| 0,5,0 | Salt | Salt Terraces | 2026-09-16 | **Fixed.** A 4-cell `ledgeSaltN` run sat between two visually-identical stretches of open salt ground (crust above, flat below, but the same dithered texture and no cliff/terrace cue anywhere near it) — not load-bearing, columns 2 and 8 already went around it. Levelled to `saltFlat` (its own tiledef's `underArt`). Two rockSand boulders, beetle and heart-piece pickup otherwise clean. `check-strands.mjs`/`check-overworld.mjs`/`check-placement.mjs`/`check-ground.mjs` all still clean, `npm run build` rebuilt. |
| 0,6,0 | Salt | Boiling Pan | 2026-09-16 | Checked at all 3 tides — the room's own code comment describes a `tidePool` ring that is walkable wet sand at LOW and water above it, holding leevers that ambush a LOW crossing; confirmed visually (tan wet-sand ring at LOW, fully flooded blue at MID/HIGH) and the leevers read as submerged/absent from the ring at MID/HIGH rather than missing sprites. Two zol clean. No defect found. |
| 0,7,0 | Salt | East Crust | 2026-09-16 | Checked at all 3 tides — `channel` (`5555`) is always rendered as water (lighter shallow at LOW, darker deep at MID/HIGH) since only its wadeability, not its wetness, changes with tide — matching the same tile's behaviour already confirmed at reef's Hooked Channel. 4 `rockSand` boulders in a 2x2 diamond (never 3 in a row); moblin clean. No defect found. |
